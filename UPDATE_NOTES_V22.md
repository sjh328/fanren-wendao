# 更新日志 · v22「归一」——信息架构 · 移动端 · 玩法全维升级

> 定位：v21 把质感拉齐后，v22 直击三大结构性痛点——**功能分散脱离主线、界面堆叠杂乱、
> 移动端只是"竖着排"**。主题「归一」：信息架构归一（子页签）、操作入口归一（移动端底部导航+双抽屉）、
> 日常归一（一键行权）、引导归一（首遇新知）。计划全文见 `docs/PLAN_V22.md`。

---

## 阶段〇 修瑕（W 组断言实测抓获的存量真 bug）

- **CSS 静默损坏修复（v19 遗留）**：v19 战斗升级区块存在一段**截断的重复坏块**，
  `.bt-sum-title { … margin-bo` 一行未闭合花括号，把**其后所有 CSS 规则整段吞掉**——
  v20 移动端触控块、v21 起的全部增量样式从未真正生效过。本次修复后：
  - 战斗结算卡、移动端 44px 触控、弹窗 94vw 等历史样式**首次真正上线**；
  - verify-v10 W 组为全项目**首个计算样式（getComputedStyle）断言组**，专门防再犯。
- **恩怨偷袭测试失效修复（v13 遗留）**：verify-v3 偷袭用例的宿敌种子走 v1 裸档合并，
  `alive` 字段丢失 → 宿敌永远不触发 → 45 轮循环沦为约 5 分钟的"伪跳过"。
  修复后偷袭真实触发，测试既变快又变真。
- **多波妖群战斗锁死修复（v19 遗留，实锤玩法 bug）**：波次刷新分支 `render()` 在
  `B.busy = false` 之前执行——第二波怪物入场时按钮带着 disabled 渲染且不再重绘，
  **手动战斗从第二波起永远点不动普攻**（自动战斗不受影响故日常难察觉）。
  v19 以来该分支特效样式恰被上条 CSS 坏块吞掉，两 bug 相互掩护；本次一并修复，
  并新增 W16 断言守护（第二波刷新后按钮必须可点）。
- **一键日常·悬赏日界 bug（本版自测抓获）**：先按旧榜单判断可领、`claim` 内部 `stateOf`
  因日界过期重新生成榜单 → "报了功没领到赏"。已改为先刷新当日榜单再领取。

## 阶段一 信息架构重组（子页签）

- **坊市拆五**（原十一区块一页平铺 → 五分栏）：
  | 分栏 | 内容 |
  |---|---|
  | 万宝阁 | 丹药/法器/功法/材料/种子 采买、出售、灵石兑换 |
  | 炼制坊 | 炼丹炉、符坊（符修）、炼器坊 |
  | 祭炼堂 | 强化、词缀洗练、本命法宝喂养 |
  | 悬赏板 | 悬赏任务板 |
  | 奇 市 | 黑市、拍卖行、布施 |
- **洞府拆三**：洞府主楼（聚灵阵+营造）/ 灵田 / 灵兽（兽栏+斗兽场）。
- **游历拆三**：舆图（地图+深耕）/ 秘境（秘境+碎片合成）/ 天下（天下大势+黄历求签）。
- **深链直达**：`act-tab` 支持 `"tab:sub"` 写法——当前建议、今日修行卡、主线 GO 表、
  焦点条全部改为 `shop:bounty`、`cave:farm`、`map:world`、`map:realm` 等直达；
  换档时子页签记忆自动复位。
- **红点随内容迁移**：悬赏可领 → 坊市红点；灵田已熟 → 洞府红点（天下大事 → 游历红点沿用）。

## 阶段二 移动端大适配

- **底部导航**：≤860px 时页签栏 CSS 重定位为固定底部栏（同一 DOM，渲染管线零改动），
  横向滚动 + `env(safe-area-inset-bottom)` 适配全面屏；主内容区预留高度不再被遮挡。
- **双抽屉**：顶栏新增「☰ 道途」「🎒 乾坤」两枚移动端入口（桌面隐藏）；
  ≤860px 时角色长面板与背包/菜单收进左右侧滑抽屉，遮罩点击即合，切页自动收起。
- **底部弹层**：≤480px 时通用弹窗/大道/天劫/战斗框改为近全屏贴底弹层，拇指可达。
- **焦点条吸顶**：移动端「主线/要务/提醒」随时可见。
- **触控复核**：子页签 chips ≥40px、底部导航 ≥44px；新手引导高亮目标在抽屉内时先拉开再高亮。

## 阶段三 玩法增补（全部挂靠既有系统）

- **宗门大比**（SectSys）：入宗后**每五年一届**，三轮车轮战（同门弟子，一轮强过一轮）；
  胜场累进发奖（贡献/灵石/气运），三连胜**魁首扬名**（气运 +8、生涯留档、年表入卷、
  新增成就「大比魁首」）；落败亦止步领已胜彩头。战斗走 Battle 既有装配，点到为止不伤根本。
- **一键行权**（今日修行卡）：按 求签 → 聚灵 → 采收灵田 → 领悬赏 顺序自动执行可办事项，
  弹一纸小账汇报所得；无可办则提示"安心修行"。
- **修炼预估**：修行卡显示「距圆满约 N 日」，"还要肝多久"一眼可读。
- **秘境路径预览**：秘境节点改为**预生成整条路线**（旧档自动补齐），界面新增「灵觉所及」
  前方两层岔口预览——肉鸽式抉择从盲选变险中求稳。

## 阶段四 剧情与引导

- **首遇新知**：当前建议新增情境化提示（法宝未佩戴/秘境已可探/悬赏已贴出/丹毒过半/
  大比正酣/功法典籍未修习），随条件自解，新系统第一时间被看见。
- **飞升结语**：白日飞升后一次性尾声演出——残玉化砂、三百年因果的收笔，
  写入大事年表「飞升 · 尾声」。
- **引导文案焕新**：新手引导五步重写（子页签分区 + 移动端入口提示），
  三分钟上手清单同步更新。

## 阶段五 交互与体验

- **弹窗关闭**：通用弹窗新增右上 ✕，**点击遮罩空白处等效取消**（结算为取消，不误触确认）。
- **左栏折叠分组**：装备法宝、境界特性收进原生 `<details>` 折叠组（默认装备组展开），
  身份卡/核心属性/当前建议等常驻项不动，长面板一眼分层。
- **子页签交互**：胶囊 chips 横向滚动、active 鎏金态、切页滚动复位与过渡动画延续。

## 阶段六 质量收口

- **verify-v10 新增 W 组 15 项断言（62/62）**：坊市五分栏、分栏内容各归其位、洞府/游历分栏、
  深链直达、红点迁移、修炼预估、秘境预览、一键日常小账、宗门大比全流程（开幕/登台/魁首）、
  弹窗 ✕ 与遮罩关闭、首遇新知、**390×844 真机视口**（底部导航贴底/双抽屉开合/底部弹层）、
  桌面 1280 布局回归。
- 受子页签影响的存量断言全量迁移：v3（秘境/天下/事件卡 → 子页签直达 + 新增子页签渲染断言）、
  v7（坊市三区块 → 三分栏断言，69 项）、verify-game（T15/T16 → 炼制坊、T13 飞升改轮询过场、
  T18 战斗前极速 ×3 防多波超预算）。
- 引导优先级修瑕（自测抓获）：新知提示原插在紧急事项之前，会把「斩三尸」等紧迫建议挤出
  前 4 条——已移至紧急事项之后。
- 九套全量回归 + `node --check`；`index.html` 缓存号递增 `?v=36`。

---

## 数值与经济

- 纯结构与体验版本：**数值、经济、掉落零改动**（大比彩头为增量发放，不影响既有曲线）。
  `balance-sim` / `price-audit` 结论不受影响。

## 升级兼容

- 老档无损：无新增 PlayerFactory 顶层字段；大比/秘境路线/新知均为懒初始化
  （`sect.tourney`、`dungeon.route` 首次触达时自动补齐）。
- 移动端样式全部为渐进增强，桌面 1180px+ 布局不变（W15 断言守护）。
