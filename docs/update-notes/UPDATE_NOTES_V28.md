# V28「重光」更新说明 —— 界面重塑 · 层级修复 · 联动织网

> 主题：v26 造壳、v27 织网，v28 重塑骨架——移动端顶栏三行并一行、底栏八键改五键+「更多」面板、
> 修复现网实锤的「点道途后全屏变暗卡死」P0 层叠陷阱，十二处系统联动补齐，界面/字号/触控全面体检。

## 一、P0 修复：移动端「道途」抽屉变暗卡死（用户截图实锤）

- **根因**：`#game-screen` 自带 `position:relative; z-index:1` 形成层叠上下文，移动端抽屉（z:80）被困其中；
  而 `#drawer-backdrop`（z:70）是 `#app` 直属子元素，在 body 层 70 > 1 → 遮罩压住包括已打开抽屉在内的
  整个游戏画面，点哪都是遮罩（变暗 + 不可操作）。
- **修复**：遮罩移入 `#game-screen` 内与抽屉同域比较（70 < 80）。已在 390×844 视口复现→修复→
  `elementFromPoint` 命中测试验证（标准视口与 110% 大字号两种场景）。
- **同类层级全库审计**：battle/popup/dao/trib/tutorial/story 等弹层均挂在 #app 层（z:100~140）> 遮罩 70，
  不受影响；`verify-v14` 新增 TB 组防回归。

## 二、界面重塑（信息架构 + 布局 + 视觉）

### 移动端 ≤860px：一屏一事
- **顶栏三行并一行（约 52px）**：左侧身份徽章「道 · 道号 · 境界」点按即开道途抽屉；右侧灵石/战力/
  ☰道途/🎒乾坤/⚙设置；气血与修为改贴顶栏下沿全宽双细条常驻（血线一眼可见）；卷章/声望/岁月/寿元
  chips 收进道途抽屉身份卡（新增「历程行」）；战力图标 ⚔ 改「武」（旧字形在多数安卓浏览器缺字渲染成 ✕）。
- **底栏八键 → 五键**：修炼/问道/游历/坊市常驻 + 「更多」；锁定页签不再一排死锁——名字保留 + 小锁角标，
  点击给出解锁提示（练气中期解锁游历/坊市，筑基解锁洞府/江湖/宗门）。
- **「更多」底部面板**：四大页签（洞府/江湖/宗门/功法）+ 十处直达格（悬赏板/奇市/万宝阁/炼制坊/祭炼堂/
  灵田/灵兽/秘境/天下/天塔），红点从页签透传到格、四格任一有事「更多」聚合亮红点；支持 tab:sub 深链、
  遮罩点击与 ESC 双路关闭，与抽屉共用遮罩互斥（防闪烁处理）。
- **行动卡合一**：主线卡 + 提醒卡两块高卡合并为一张行动卡上下分层（移动端），高度近乎减半。

### 桌面 ≥861px：一屏全貌
- **乾坤袋**：头部新增家资摘要行（灵石三档/声望/贡献）；物品列表改双列网格（排布更满、扫读更快）。
- **修炼页尾部「诸处速达」**：12 处常用目的地胶囊一键直达，红点透传（双端通用，桌面中栏留白显著减少）。
- **视觉令牌统一**：新增 `--radius-card(12px)/--radius-pop(16px)/--card-pad` 令牌，卡片/面板/弹层/按钮
  圆角与内边距统一派生；弹窗圆角统一 16px。

### 双端
- **界面密度档**：设置中心新增「舒适/紧凑」，紧凑档卡片内边距与间距收紧（约 +15% 内容），随档持久化。
- 修炼页身份卡新增历程行（卷 X/10 · 历时 · 声望档位）。

## 三、系统联动织网（十二处，全部「现有系统接线」，无新玩法）

| # | 联动 | 效果 |
|---|------|------|
| G1 | 气运 → 黄历求签 | 凶签权重 ÷ 气运乘数（气运 100 → 下签出现率约减半） |
| G2 | 声望 → 黑市 | 买价吃 RepSys.priceMul（±15%）；结交初始好感 ±（名望高者人缘更好） |
| G3 | 装备福缘 → 判定总线 | 驯兽率/黑市还价/郎中免费/黑市陷阱四处改读有效福缘（本命法宝等装备 luck 不再蒸发） |
| G4 | 心魔 → 突破成算 | 未降伏的心魔每 10 点 -1%（满百前最多 -9%；破而不修有代价） |
| G5 | 贡献 → 听讲一日 | 宗门贡献兑换新增可反复购买项：300 贡献 → 感悟 +8，日限一次（长老期贡献溢出有去处） |
| G6 | 悟性/气运 → 拍卖 | 激进出价档吃「鉴宝眼光」（封顶 +15%），弹窗显示与实算一致 |
| G7 | 感悟溢出 → 修为 | 新增 `Cultivate.addInsight` 单源入口：满百后溢出按境界经济折算修为（讲道/心魔劫/个人线/前世机缘/上签五大额源接入） |
| G8 | 丹毒 → 成丹率 | 丹毒过上限五成 → 成丹率 -5（「手有浮毒，丹火不稳」；丹修道途天然抗毒呼应） |
| G9 | 塔绩 → 转世 | 跨世登塔最佳入 legacy：≥10 层来世气运 +5，≥20 层再 +1 悟性（登塔足印不随轮回散去） |
| G10 | 气运/交情 → 洞府访客 | 触发率吃气运乘数；rel≥60 莫逆登门另有厚礼（上品灵材 + 灵石） |
| G11 | 亲昵 → 灵兽出行 | bond≥60 的灵兽出行多衔一份材料 |
| G12 | 悟性 → 灯谜 | 上元灯谜正误率 40+悟性×2（封顶 85%），替换原 50% 固定掷 |

## 四、功能升级（不加新玩法）

- **一键行权扩容**：已达成的宗门任务随一键行权自动领取（此前只到悬赏）。
- **设置中心**：界面密度档（见上）。
- 后台页签省电：页面隐藏时停古琴循环，回前台自动续上。

## 五、回归与验收

- 新增 **verify-v14** 套件 33 断言（P0 层叠回归 / 更多面板导航 / 单行顶栏 / 联动数值 / 听讲 / 密度档）
  接入 `test:all`；同步更新 verify-v4/v5（页签数 9）、verify-v11（单行顶栏与锁定新预期）、
  verify-v12/v13（缓存号 v=44、SW v3）断言；verify-v7 DAO3 改显式必成（G8 丹毒惩罚上线后消除统计性偶败）、
  verify-v10 E2 度量前归零修为状态（E1 重置境界后溢出转存会使 totalExp 度量失真——测试度量瑕疵，
  非 游戏缺陷）。
- 全量 13 套件 **562 断言 0 失败 0 控制台错误**（连续两轮）；缓存号 `?v=44`、SW `fanren-wd-v3`。
- 双视口六屏截图 + judge 视觉验收全过（桌面三栏/问道/坊市 + 移动端单行顶栏/道途抽屉打开态/更多面板）。

## 六、已知取舍

- 桌面左栏「当前建议」与行动横幅的信息重复**保留**（v27 刚按「建议提前」重排过，不宜再翻烙饼）。
- 剧情字号不再单列档位——根级 zoom（v27）已整体缩放全界面，包含剧情页。
- 斩三尸/突破预估等旧文案数值不变；心魔减成与「挫而愈坚」方向相反属有意设计（心魔是债，连败是砺）。
