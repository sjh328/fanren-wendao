# V31「登仙」计划 —— 仙界四阶 · 印记归真 · 战斗纵深二阶 · 工程修缮二批

> 用户诉求（2026-09-15）：全方位检查游戏、优化游戏逻辑、修复 bug、对功能深度大升级、改善不合理与薄弱处，做一次全方位大升级；先出完整详细计划，再按计划执行，不要遗漏。
> 方法论：沿用 v30 六路并行深审（战斗灵兽塔 / 装备炼器坊市 / 修炼轮回天劫 / 社交内容剧情 / UI 工程基建 / 洞府日常探索），全部指控在 v30 权威源码（master@ed5f62a）上逐条复核；十四处 P0 已由主审二次实锤确认。
> 版本代号「登仙」：v30 让飞升有了真判定，v31 让飞升不再是终点——白日飞升之后，地仙→天仙→金仙→大罗的仙界四阶循环正式开门（v30 留档的头号后置项）；「登仙」亦指十一处 P0 逐一「登门修仙」：死去已久的机制面（宗门特殊兑换、助战、精英基线、塔层奖额度）在本轮全部复活。
> 完整计划见 `docs/PLAN_V31.md`，执行落档见 `UPDATE_NOTES_V31.md`。

---

## 〇、审计基线与执行护栏

- 基线：master@ed5f62a（v30 收尾）干净；`npm run test:all` 15 套件全绿（EXIT=0）已复核。
- v30 工程护栏沿用：改 `js/` 源码 → `node scripts/build.mjs`（自带 check-sync 分叉防呆）→ 快速回归 `node verify-game.mjs`。
- 每批次完成即快速回归；全部完成后全量两轮 + judge 视觉验收 + 一次性 commit（post-commit 自动双推）。

## 一、审计总结（六路深审结论）

- **战斗灵兽塔**：v30 合击 2.0 与「手工精英统一口径」存在两组致命落地缺陷——灵兽助战/驯服调用根本不存在的方法（`B.pushFloat/B.log`，助战必抛异常、驯服成功即软锁）；`buildMonster` 的 elitePlus 修复写在 `return` 之后且改错对象（秘境/魔域精英整体退化为隐形精英）。另有塔层奖日额度永不重置（满后永久归零）、新状态「破绽」无衰减处理器、诅咒祝福产出 2.5 件小数物品污染存档、塔影可驯服+驯服不走层结算（无限驯兽自助餐）四枚 P0。P1 十三条（反制读招的 lastAct 永假、敌方被缚漏玩家时序、福缘灵石文案造假等）。
- **修炼轮回天劫**：印记去重集 `marksGiven` 随 `Meta.load` 重建被丢弃——重进游戏后图鉴/个人线/飞升印记全部可跨世重刷，多周目经济崩坏（P0）。闭关循环缺 `Battle.active` 守护（v30 注释自称「对齐 autocult」实缺半边）。P1 六条（轮回镜世数错位一世、渡劫护法负收益、传承树十层「逆天改命」晚一世生效、飞升仙劫不吃渡劫丹、感悟折修为 silent 连跳小层无播报等）。仙元基础设施盘点完毕：全量正修为走 `addExp` 单口，溢流入口覆盖完整；`LIFESPAN[9]=99999` 使真仙后寿元轴死区——正是仙界四阶要承接的。
- **社交内容剧情**（主审自审）：**个人线终章灵石加成口径倒挂**——`PersonalSys.bonusOf` 聚合是加法（起始 1、`+= v`），而 n8「秤心」`stoneMult:1.05`、n19「成色」`1.04` 写成乘数形式，实发 +105%/+104%（设计意图 +5%/+4%），终章文案也按 `v*100` 显示 +105%——全局灵石收入隐性翻倍以上的印钞口（P0）。悬赏连锁Ⅲ赏格文案 ×3 实发 ×2.8（`CHAIN_MUL` 表定义了 3 却未被消费）。剧情引擎 `_vis` 旗标条件已就位，多周目变奏可低成本实现。
- **洞府日常探索**（主审自审）：洞天营造/亲传弟子历练/图鉴分档/悬赏连锁自洽；灵田收获材质层级封顶 tier4 与赠礼 tier5 的分层差异属设计现状；无新增 P0。
- **UI 工程基建**：v30 新功能「宗门特殊兑换（侠名帖/器魂匣）」按钮 `data-action="sect-exchange"` 无处理器且参数名错——**整个功能从上线起就点不动**（P0）；v29「教程完毕再开剧情」的修复在真首机上失效（`Tutorial.show()` 展示路径不清 `onDone`，紧跟的无条件回调让剧情在教程层下提前叠开）（P0）。P1 十三条：guide 泄漏内部术语、manifest shortcuts 深链死链、新版本提示条静默落空、删档孤儿键、存档弹窗可删 auto 且文案 NaN、act-save 切槽 Meta 分叉、日志过滤器组缺入口、成就解锁二次全量渲染、日志回前台不吸底、无障碍（toast 无 aria-live、两处对比度 <4.5:1）。
- **装备炼器坊市**：三枚 P0——**装备格挡 block 是死键**（词缀/套装/强化按功能键收 block 费、明细表虚报，compute() 却只吃 gf.block 不读 eq.block，v27 修瑕系列唯独漏了它）；**器魂重铸的后缀半边是死代码**（后缀定义无估值，affixScore 恒 0 → rollBetter 后缀永不变化，「双缀同洗保底不降」对已带后缀的装备收费零服务）；**洗练「保底不降」对后缀失效**（同根：后缀可从夺魄洗成吸血严格降级，v30 头号卖点半边落空）。P1 十二条（强化石旧「必成」文案未随 v30 清理、affixKept 丢弃不清理与「无法找回」矛盾、画符失败也烧次数且显示价恒为首档、赌袋基础结构恒正期望、拍卖可掷出不可竞拍的拍品锁死整期 60 日、坊市 3 倍实为阶跃函数、洗练估值忽略两段式 per×grade、分解面板漏报器魂等）。

## 二、A P0 十一连速修（全部实锤，合计约 1 天）

| # | 病灶（证据） | 处方 |
|---|---|---|
| A1 | **个人线灵石加成口径倒挂**：`PersonalSys.bonusOf` 加法聚合（npc.js:851 起 1），n8 `stoneMult:1.05`/n19 `1.04` 写成乘数——实发 +105%/+104%，fxText 也显示 +105%，全局灵石 ×2 隐性印钞 | 数据改加法口径 `0.05/0.04`（与 n5/n12/n15 对齐）；verify 锁 fxText 输出 |
| A2 | **宗门特殊兑换死按钮**：ui.js:1421 `data-action="sect-exchange"` 无处理器、参数名 `data-idx` vs 动作表读 `d.i`——v30 侠名帖/器魂匣整体不可用 | 按钮改 `data-action="act-exchange" data-i="${i}"`；运行时断言封死 |
| A3 | **教程/剧情双层叠开**：game.js:170-175 无条件消费 `Tutorial.onDone`，而 `Tutorial.show()` 展示路径不清 onDone——真首机剧情在教程层下提前叠开，v29 修复失效 | `show()` 展示路径内清 `onDone`（早退才悬空由外部接力）；或 show 返回是否早退 |
| A4 | **轮回印记可无限重刷**：`Meta.load`（meta.js:13-17）重建时丢弃 `marksGiven`——重进游戏后图鉴五类/个人线/飞升印记全部可跨世重发，直通全属性与传承树 | `marksGiven` 移入全局 legacy（跨世单键）；`grantMarks` 改读写 legacy；旧 Meta 键数据首次读取时并合迁移 |
| A5 | **连续闭关缺战斗守护**：cultivate.js:237 守护缺 `Battle.active`——循环间隙点探索开战后循环继续烧灵石岁月 | 守护补 `Battle.active`（兑现注释声明的「对齐 autocult」） |
| A6 | **灵兽助战/驯服调用不存在的方法**：beast.js:65/143/172 调 `B.pushFloat/B.log`，而 `Battle.active` 是纯数据对象——助战掷中即抛异常（被吞成「气机紊乱」行动作废）；驯服成功即软锁（`victoryTame` 不执行、`B.busy` 锁死） | 改 `Battle.pushFloat/Battle.log`（与 tower.js:202 同款）；补 assist 全链路运行时断言 |
| A7 | **手工精英基线死代码**：status-fx.js:35 已 `return`，:38 `elitePlus` 不可达且 `m` 是取整函数非对象——秘境/魔域/夺宝精英失去 10% 暴击基线/狂暴/计数，v30「统一口径」实为零生效回归 | elitePlus 逻辑移入 return 前对象构造（`elite: e \|\| !!opts.elitePlus`、crit 同步）；删死分支 |
| A8 | **塔层奖日额度永不重置**：tower.js:77-81 换日只清 `used/bought` 不清 `today.stones`——历史层奖累计达 300×eco 后每日层奖灵石恒 0，与「归于明日」文案相反 | `syncToday` 补 `t.today.stones = 0` |
| A9 | **「破绽」无衰减**：status-fx.js 两侧衰减表均无 `vuln`——一张破阵符=敌方永久 +30% 被会心，标签恒「余2回合」 | `vuln` 入敌方侧衰减表（AUG_ENEMY）；顺手清 `decayKinds` 遗留死代码 |
| A10 | **诅咒祝福产小数物品**：tower.js:36 `chest: 2.5` → :324 `n = 1+(mods.chest-1)` = 2.5 件入包污染存档 | 数量取整 `Math.max(1, Math.round(...))` |
| A11 | **塔影可驯服+驯服不走层**：canTame/tame 不排除 `ctx.tower`，驯服胜利不走 `TowerSys.onVictory`——一次登塔次数内同层无限重踏无限驯兽 | 驯服入口排除塔战斗（塔影为气相投影不可驯，按钮不显示）；文案补一句设定 |
| A12 | **装备格挡 block 死键**：stat.js:133 只吃 `gf.block` 不读 `eq.block`——词缀（磐石）/套装/玄天玉佩/仙缘玉环的 block 全部无效，强化按功能键收费、明细表却虚报该来源 | compute() 补读 `eq.block`（与 v27 修瑕系列同款补漏）；verify 锁数值 |
| A13 | **器魂重铸后缀半边死代码**：后缀定义无 `bonus`，affixScore 恒 0 → rollBetter 对后缀恒 `0>0` 假——已带后缀的装备花 8 器魂后缀永不变化，弹窗却宣称「每侧保底不降」 | 后缀定义补 `score` 标量估值（三处共用，见 D1 根修） |
| A14 | **洗练保底对后缀失效**：同根——`oldScore>0` 恒假，后缀可被洗成严格降级（夺魄→吸血） | 同 A13 根修：后缀估值落地后 reroll/recast/bag 对比三处自动生效 |

## 三、B 真 bug 批修总表（P1/P2，按线分组）

### B1 战斗与灵兽（审计证据齐全）

| # | 修复 | 位置 |
|---|---|---|
| E1 | 反制读招的 `B.lastAct` 永假（全工程零赋值）——act 命中后置值，AI 2.0 反制真正生效 | battle.js:589; beast.js:137 |
| E2 | 敌方被缚回合漏玩家侧时序（`tickDots('me')`/不灭金身/`B.turn` 不增——冰封链可无限拖力竭、打歪速胜/无伤口径）——分支复用 `afterEnemyPhase` | battle.js:1138-1149 |
| E3 | 玩家被缚分支缺敌方死亡判定（DOT 击杀后尸体多站一回合） | battle.js:593-605 |
| E4 | 「福缘深厚，额外掉落灵石一袋」文案造假（无任何实发）——真给 10% 加成或删句，定为真给 | battle.js:1532-1535 |
| E5 | 伤害统计漏计（剑气余韵/法相/追雷不入 `B.stats.out`、破招追加不入 src.attack）——统一汇入 | battle.js:652,674-684,818-822 |
| E6 | 多波战斗「每战一次」标记不重置（comboUsed/bmUsed/infantSaved/jadeSaved）+ mercy/灵压/慑魂只作用首波——续波统一 | battle.js:1450-1477 |
| E7 | 塔层事件优先级遮蔽（15/30/45 宝箱吞祝福、35/70 吞奇遇，与头注承诺不符）——并列触发 | tower.js:243-247 |
| E8 | 塔灵赐福可随机送诅咒祝福——gift 池过滤 curse | tower.js:255-263 |
| E9 | 宝箱层「气血回复三成」死文案——按 healChest 模组动态生成 | tower.js:332 |
| E10 | 塔绩兑换价与境界经济脱钩（雷晶核 60 塔绩高境两日一枚白拿）——兑换耗绩随境界缩放 | tower.js:44-49 |
| E11 | 精英词缀「镜像」只认丹药 buff（法诀 buffDef/符箓增益全不触发）——挂到 StatusFx.add 统一入口 | battle.js:866-870 |
| E12 | 敌方 dodge 修好了却无内容承载（全 MONSTERS 仅 1 只有 dodge）——速攻系/高 power 妖兽批量补配 | game-data.js MONSTERS |
| E13 | 悬赏连锁Ⅲ赏格 ×3 实发 ×2.8（`CHAIN_MUL[3]=3` 从未被消费）——统一走表 | bounty.js:76-82 |
| E14 | 灵兽派遣在途可被设出战/斗兽（`setActive` 不查 `b.trip` 双重收益） | beast.js:219-225 |
| E15 | 势尽加成可被防御跨回合保留（免费囤 buff）——防御不清 `lastSkillTag` 的口径修正 | battle.js:623 |

### B2 修炼与轮回

| # | 修复 | 位置 |
|---|---|---|
| E16 | 轮回镜「第 X 世将至」错位一世（`lives+(lives?0:1)` 只在 0 时正确） | reincarnation.js:69 |
| E17 | 渡劫护法负收益：劫体（realmIdx≥8）吞掉 aid 保留率分量且感悟反少 5——aid 分支单独保留率 0.9 | tribulation.js:206-216 |
| E18 | 传承树十层「逆天改命」晚一世生效（rollAttrs 读前世旗标）——execute 按新 treeTier 先置位再掷 | reincarnation.js:144,170 |
| E19 | 飞升仙劫不吃渡劫丹（普通天劫扣丹 +5、飞升无）——同款扣丹 | cultivate.js:366 |
| E20 | 感悟溢出折修为 silent 连跳小层无播报——进层播报与溢出日志分离 | cultivate.js:70 |
| E21 | 渡劫丹「失利不返还」死文案与回溯自洽性——文案改「回溯可返、连败以孽障计价」 | tribulation.js:41 |
| E22 | 传承树效果散落 10 个 if——重构 effects 单源（顺带修三层连环） | reincarnation.js:157-176 |

### B3 社交内容与 UI 工程

| # | 修复 | 位置 |
|---|---|---|
| E23 | guide.js 要诀泄漏内部术语（blessings/grade）——改中文 | guide.js:14,18 |
| E24 | manifest shortcuts 深链 `?tab=` 无消费——Game.init 解析并跳页 | manifest; game.js init |
| E25 | 新版本提示条在游戏内时静默插入隐藏的开始界面——循环等待开始界面可见或改 toast | index.html:246-247 |
| E26 | 删档留孤儿键 `meta_<slot>`——`Save.remove` 联动删 meta | save.js:68-70 |
| E27 | 存档弹窗可删「自动存档」且确认文案「存档位NaN」——auto 行隐藏删除钮 | ui.js:1825; game.js:526 |
| E28 | `act-save` 中途切槽不重载 Meta（成就写进新槽分叉）——act-save 后 `Meta.load()` | game.js:510-517 |
| E29 | 日志过滤器 system 组无按钮——补「系统」过滤钮 | log.js:74-81; index.html |
| E30 | 成就解锁二次全量渲染——只 markDirty 局部 | achieve.js:105 |
| E31 | 日志后台回来不补吸底——visibilitychange 回前台按 paused 补滚动 | log.js:101-108 |
| E32 | 无障碍批修：toast/announce 补 aria-live；`.log-info`/`--grade-0` 对比度加深至 ≥4.5:1 | index.html; style.css |
| E33 | 移动端「已自动存档」save-dot 在 `.m-hide` 内不可见——挪出或在迷你条区显示 | ui.js:117 |
| E34 | achieve「斗兽十连胜」文案与累计十胜口径对齐；npc.js「終认」繁体字 | achieve.js:65; npc.js:682 |

## 四、C 仙界四阶循环（版本主题·头号升级）

真仙圆满不再是终点：v30 的「仙元」从死账升格为仙界四阶循环的资粮。

1. **数据结构（独立轴）**：`p.xianjie = { idx: 0..4, exp: 0 }`（0=未入/1=地仙/2=天仙/3=金仙/4=大罗），fresh 模板 + migrate `||{idx:0,exp:0}` 自愈；不动 realmIdx 0-9 假设（敌人 realmLabel/天骄榜/塔全硬编码九境）。
2. **仙阶经验**：`XIAN_TIERS` 四阶，每阶 3 层；层需仙元按 4/8/16/32 千起步逐阶 ×2（终局全程约 60~100 次修炼动作/若干轮闭关——先在 balance-sim 校准溢出速率再定稿）；仙元入口唯一（addExp 溢流）已覆盖完整，不改。
3. **仙阶晋升=仙劫**：`Tribulation.run` 参数化 `target` 与表（power 公式外推 +10/阶、realmPenalty 续算、护甲需求升至「仙级」（GRADE_NAMES 已备 5 档））；借天运/渡劫丹/三策/异象零改动复用；仙劫失利折仙元三成（不折寿——仙寿另计）、可再叩。
4. **属性收益**：每层全属性 +1.5%（finalScale 乘子），四阶圆满 +18%；仙阶越大，`baseGain` 附加仙阶修炼效率加成（每层 +2% 修为）。
5. **寿元承接**：入仙阶即「仙寿」（lifespan 按阶 +2000/5000/12000/30000 年续接——把 LIFESPAN[9]=99999 的死区变成新刻度）；仙劫失利折寿改为折仙元，天年告警条适配。
6. **UI**：修炼页飞升卡改双态（未飞升=引劫飞升；已飞升=仙阶晋升卡：当前阶/层/仙元条/晋阶按钮）；仙途条十境之后接四阶节点；顶栏境界徽章追加仙阶字（地/天/金/罗）；属性明细卡补「仙阶」来源行。
7. **仙界内容钩子**：`dailySettle` 挂「仙界访客」（仙缘童子赐仙元/仙官论道给感悟/仙兽寻主——(p, auto) 模式离线静默）；仙界轶闻池随仙阶扩写；「仙眷」REALM 特性后续写四条仙阶特性文；大罗圆满触发「道祖之境」一次性大奖（印记 +1、专属称号、全属性再 +3% 旗标）。
8. **转世处理**：仙阶属「此世仙籍」不随轮回（与道境同口径）；轮回镜补「仙籍」行（legacy 记历世最高仙阶，跨世展示）。

### B4 装备炼器与坊市

| # | 修复 | 位置 |
|---|---|---|
| E35 | **后缀估值根修（A13/A14 同源）**：10 条后缀补 `score` 标量估值与 `per`（随品阶两段式成长），reroll/recast/bag 对比三处共用 | forge.js; game-data.js 后缀表 |
| E36 | 强化石旧「必成」文案清理（game-data 与 ui 两处）改「成功率+40%并积祝福」 | game-data.js:330; ui.js:1207 |
| E37 | affixKept/enhBless 丢弃口径统一：drop/dropCategory 与 salvage 三键同清（兑现「无法找回」） | bag.js:296-324 |
| E38 | 画符失败不烧次数、按钮显示当前档实价 | craft.js:142-147; ui.js:1159 |
| E39 | 器魂重铸空手保底：空侧至少出一条前缀 | forge.js:283-285 |
| E40 | 赌袋期望收敛：中奖档 2 份→1.6 份（幸运 0 期望转负、高运转正） | black.js:108-122 |
| E41 | 拍卖按当前境界过滤 LOT_POOL（不再掷出整期不可竞拍的拍品） | auction.js:48-53 |
| E42 | 洗练/重铸估值纳入两段式 per×grade（按境界折算百分比词缀期望值再比较） | forge.js:193-198 |
| E43 | 套装面板文案补「两件另享仙器共鸣」 | game-data.js SETS |
| E44 | 分解面板补器魂产出行 | ui.js:1252-1256 |
| E45 | 黑市补 m_leijing/m_xiancui 上架（f19-f22 与失传丹方的中期料源）；仙灵种 minRealm 8→6 | black.js; game-data.js |
| E46 | DonateSys 缺位回退死分支删除（内嵌旧 2.2^r 曲线防未来误启用） | ui.js:1008 |
| E47 | 坊市直购价 3 倍阶跃改线性爬坡（1+0.66×境差，封顶 3 倍） | shop.js:19 |
| E48 | price-audit.mjs 扩三采样点（r0/6/9）+ 炼器期望成本对照 | scripts/price-audit.mjs |

## 五、D 战斗纵深二阶（升级二）

1. **连招记忆**：新增「对招」组合表——盘内特定二连（起手+终结位）触发专属追击「连珠」+12%；同盘 3 连以上锁链递增 +4%/连（上限 +12%）；连招状态在战斗头部显示当前「势」。
2. **合击付费化**：人兽合击从「每战一次」改「战意 + 真元付费充能」（首用免费，后续 3 战意 2 真元一次）——多波战斗与长战不再浪费；合击伤害吃连击层。
3. **控制递减**：同一目标连续受同类控制（冰封/束缚/迟滞）递减 15%/次（下限 40%）——消除「无限冰封链」；顺带修 E2 的时序后重构该分支。
4. **天塔规则祝福**：祝福池补 3 条改变打法的规则祝福（蓄力必可破招/DOT 翻倍但每层自损 2%/击杀回真元 2），塔守 Boss 增加阶段技（血量 50% 以下解锁专属一击）。
5. **AI 针对性应对**：`playerMoves` 管道升格——玩家高频法诀时狡诈型加魔抗摄灵、高频普攻时铁壁型提格挡（读招博弈再深一层）。
6. **力竭预告**：意图栏预告「第 X 回合：久战力竭」——节奏机制对玩家可见。
7. **装备纵深（E35 根修的顺延）**：词缀稀有度（高端前缀加 `w` 权重与 `minGrade` 门槛，重铸连掷同吃）；词缀实际值可视化（affixText 标注本件两段式实值）；强化面板补「祝福 b/100」徽标与「连祭炼×5」；炼器失败保底「器胚残片」（集齐可抵一次材料——全游戏唯一无 pity 的大额赌博补上保底）。

## 六、E 灵兽与江湖纵深（升级三）

1. **亲昵成长线**：亲昵 ≥80 解锁第三天生技槽（可学第二资质技）、≥100 合击伤害 +25%；「法诀呼应」升格为可见「默契值」随呼应次数成长并小幅反哺亲昵。
2. **寻宝事件化**：灵兽派遣归来从纯公式改「三选一归来事件」（奇物/灵石/亲昵），保留保底。
3. **多周目剧情变奏**：兵解转世者携「前世残忆」旗标（`p.reinc.lives>=2` 自动 + legacy 跨世去重），c2/c5/c7 挚抉章节出第三变体场景（_vis req 旗标路由，约 6 段新文案）——「重开一世」第一次在剧情里被看见。
4. **印记消费端（轻）**：轮回镜补「来世预约」——花费印记定制来世（如 6 印记=来世自带仙缘玉环、4 印记=来世初始境界练气三层），被动加成第一次变主动构建（依赖 A4 修复先行）。

## 七、F 工程与体验「地基二批」（升级四）

1. **设置项补全**：设置卡补「重看新手引导」（清 fanren_wd_tutorial 重播）、「日志条数上限」档位、存储占用展示（navigator.storage.estimate 可用则显）。
2. **存档管理**：导入补 `<input type=file>` 通道（.txt 直接导入，文本码保留）；存档卡显示 bak2 快照时间。
3. **成就扩容（轻）**：补 6 项——宗门派系线 1、洞天营造 1、本命喂养 1、声望段位 1、仙界四阶 2（金仙/大罗）；奖励引器魂档。
4. **渲染微优化**：江湖页 NPC 行内容指纹跳过（portrait SVG 不再每次全量重画）。

## 八、J 验收

1. **verify-v17 新套件（≥100 断言）**：A 组（P0 十一连：stoneMult 口径/特殊兑换处理器/教程接力/marksGiven legacy 化/闭关战斗守护/assist 不抛异常/精英基线/塔额度重置/vuln 衰减/贪匣取整/塔不可驯）、B 组抽样（lastAct 赋值/被缚时序/连锁Ⅲ ×3/护法保留率/逆天改命当世生效/仙劫吃丹）、C 组（仙界四阶：轴数据/仙元消费/仙劫判定/属性加成/寿元续接/大罗毕业/转世清籍/轮回镜仙籍）、D~F 组（连招记忆/合击充能/控制递减/规则祝福/亲昵三技/前世残忆变体/来世预约/设置项/导入文件）。接入 `test:all`。
2. 受影响旧套件按需修订（v3/v10/v16 等数值与文案断言）。
3. **全量 `npm run test:all` 16 套件全绿、0 控制台错误（连续两轮）**（先 `npm run serve`）。
4. 双视口六屏截图（`scripts/shoot-v31.mjs`）+ judge 视觉验收（重点：仙阶晋升卡/仙途条四阶节点/轮回镜仙籍行/来世预约/设置卡/特殊兑换按钮可点，移动端不回归）。
5. UPDATE_NOTES_V31 落档、playbook.md 更新到 v31、缓存号 `?v=47`、SW `fanren-wd-v6`；**最后一次性 commit**（post-commit 自动双推）。

## 九、兼容与风险控制

- 新持久字段（`p.xianjie`、legacy.marksGiven、legacy.xianjieBest、来世预约标记、规则祝福 id）一律 fresh 模板 + 读写 `||0/||{}` 自愈；A4 迁移只并合不回写旧键。
- 仙界四阶数值双护栏：`scripts/balance-sim.mjs` 先跑仙元溢出速率模拟定层需 → verify-v17 锁 → 全量两轮。仙劫复用天劫三策，不新增判定系统。
- A4（印记重刷）修复会使部分玩家「重进重刷」的既得印记回落——属堵漏正确方向，UPDATE_NOTES 明示。
- D 组（战斗纵深）每项独立可回退；连招记忆/合击付费化不改自动战斗默认策略。
- 每批次完成即 build + verify-game 快速回归。

## 十、执行顺序

A P0 十一连 → B 批修（E1~E34）→ C 仙界四阶（先模拟后落地）→ D 战斗纵深 → E 灵兽/江湖纵深 → F 工程地基 → verify-v17 + 旧套件修订 → 构建全量回归两轮 → judge 视觉验收 → 落档一次性 commit。

---

### 附：本轮审计基线

- master@ed5f62a（v30 收尾）干净；六路深审（战斗灵兽塔 / 修炼轮回天劫 / 社交内容剧情【主审自审】/ 洞府日常探索【主审自审】/ UI 工程基建 / 装备炼器坊市）+ 主审十一处 P0 二次实锤。
- 全量回归基线：`npm run test:all` 15 套件全绿（v30 验收口径，EXIT=0）。
