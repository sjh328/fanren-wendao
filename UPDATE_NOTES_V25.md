# UPDATE_NOTES_V25 ——「掌上乾坤」

> 版本主题：**移动端全面重构 + 登天塔新玩法 + 真仙终章**。
> v24 全量移动端审计（390×844 实机截图）暴露的结构性问题本轮全部清账：
> 剧情演出弹窗自 v15 起就没有纸张背景（文字浮在模糊页面上，全平台破损）；
> 顶栏 chips 在 390px 溢出被裁；底部导航八页签带锁即爆；教程结束后右抽屉不收起，
> 新玩家开局第一屏被乾坤袋盖住；Toast 同帧连发叠罗汉；仙途十境条真溢出裁切。

## 阶段 A 移动端体验重构（本轮重点）

- **A1 剧情演出修复 + 沉浸化**
  - `.story-box` 补双层宣纸卡底 + 圆角 + 阴影（与 popup-box 同款）——全平台修复，桌面剧情终于有"卷轴"实体；
  - ≤860px 剧情沉浸态：播放时 `body.story-playing` 隐藏底部导航与顶栏，剧情卷轴全屏化
    （`100dvh` + 安全区内边距 + 自身滚动），播毕自动摘除；剧情中不再看见页签探头。
- **A2 顶栏两行化（≤860px）**：上行 标题+道途/乾坤/设置，下行 `#top-info` chips 独占一行
  横向滑动（渐隐 mask + 隐藏滚动条 + 不换行）——390px 再无裁切；桌面一行布局原样。
- **A3 底部导航自适应**：页签 `flex:1 1 0; min-width:0` + `clamp(11px,3.1vw,14px)` 字号，
  320~860px 全宽等分；**锁定页签只渲染 🔒 不渲染文字**；红点改绝对定位角标不占宽；
  v24 的右缘雾化 mask 退役（不再需要）。
- **A4 教程抽屉自愈**：教程不再在移动端自动拉开抽屉（避免收不回去）；`Tutorial.finish`
  统一 `UI.closeDrawers()`——开局第一屏永远从完整主界面开始。
- **A5 Toast 队列化**：同屏至多 3 条，超出移除最旧；位置移至底部导航上方，全程不挡顶栏按钮
  （`pointer-events:none` 本就有）。
- **A6 仙途条 / 问道 rail 适配**：仙途条包 `.rp-scroll` 横滑容器（渲染后自动把当前境界
  `scrollIntoView` 居中）+ 窄屏渐隐提示；问道九章轨道同款渐隐。
- **A7 触控与表单**：≤860px 全部 `input/select/textarea` 字号 ≥16px（iOS 聚焦不再放大整页）；
  背包行内按钮 / 日志工具按钮高度 ≥36px；战斗主按钮 ≥44px。
- **A8 弹窗滚动锁**：`body:has(.modal:not(.hidden)) { overflow:hidden }`（渐进增强，旧内核无感降级）
  + `.modal` 与剧情卷轴 `overscroll-behavior:contain`——弹窗开着时背景不再跟着滚。
- **A9 渲染性能**：`#tab-content` / `#log` 加 `contain: layout style`，重渲染重排范围隔离，低端机减负。
- **A10 平板双列**：641~860px 平板竖屏修炼页启用双列栅格，不再一条长到底。

## 阶段 B 新玩法 · 登天塔（TowerSys，js/systems/tower.js）

- **B1 入口与次数**：游历子页签第四栏「天 塔」；筑基期解锁。每日免费 1 次，灵石加购 1 次
  （成本 `80 × stoneEco` 随境界水涨船高）；最高层双纪录——本档 `counters.towerBest`、
  跨世 `Meta.data.towerBest`（存档导出文本码随行携带）。
- **B2 爬塔循环**：每层一头「塔影」守影（本档妖兽池随机 + 词缀习性继承，强度按
  `境界×4 + 层数/2` 缩放封顶 60）；**气血跨层延续**；胜利层奖立即入囊（灵石随层数爬坡 +
  半额修为）；每逢 3 层塔心赠福三选一，每逢 5 层开宝箱 + 回复三成气血；层间可「收手离塔」
  带走全部收获；**败北止步不出人命**（血线抬至三成，无灵石修为折损），遁走等同离塔。
- **B3 塔心祝福 15 种**：慑魄低吟/碎甲罡风/蚀灵血煞/迟滞咒纹/塔灵低语/点石成金/聚宝盆纹/
  顿悟钟声/壁上残篇/回春玉露/深泉心露/剥灵之手/避劫福纹/破釜沉舟/金刚护体——全部实现为
  敌人侧数值模组与结算乘数，**零侵入战斗公式**，战报明示祝福生效行，出塔即散。
- **B4 战斗接线（最小侵入）**：`Battle.victory/defeat/flee` 各加一个 `ctx.tower` 分支
  （对齐既有 tourney/story/dungeon 模式）；塔战不走普通掉落结算，层奖单源化由 TowerSys 发放。
- **B5 收录与激励**：成就 4 档（登顶 5/12/20/30 层：初登天塔/拾级而上/塔影同高/塔顶之风）；
  支线 s21「塔铃声又响」；塔产奇物 3 件（天塔灵砂/云阶铁/镇塔符核，宝箱掉落可收藏可出售）；
  天骄榜补「登天塔本档/跨世最佳层」参考行；帮助手册新增「登天塔」一章；Guide 新知
  「城西登天塔开塔了」按条件出现；问道页「前往」直达 `map:tower`。

## 阶段 C 内容拓展 · 真仙终章

- **C1 第十章「仙门之外」**：主线九章 → **十章**。c9 `supR` 999→10（真仙即追认，长线不再烂尾），
  新 c10 `supR:999` 接棒终章——星海长阶、问道门阙、守门人三关（真仙之境 / 登天塔十层塔影照心 /
  斩妖证道），章末 **门前影·两世之我** 剧情战：胜则门开、三途尾声抉择（问路/问我/不问）+
  **残玉终响（全属性永久 +3%，`flags.beyondGate` 接入 Stat.finalScale 与明细）**，
  败则「未竟」收场留有余地；玉灵道别一场收束全程陪伴。
- **C2 守门人**：新角色 `c_gatekeeper` 注册 CHARACTERS（声音"像采药老人，又像三百年前的自己"），
  立绘/名帖按单一事实源接线。
- **C3 抉择树**：问道录抉择树补 `c10_end` 三途标签；CN9 补「十」；全卷完成文案更新为
  「问道十章 · 全部完结」。

## 阶段 D 质量收口与发布

- **verify-v11 新增 AA/AB/AC 三组 26 断言**：
  - AA 移动组（390×844）：剧情纸张背景 / 沉浸态隐层与摘除 / 顶栏两行 / 锁定图标化 /
    教程抽屉自愈 / Toast ≤3 / 表单 ≥16px / body 滚动锁 / 仙途条自动定位；
  - AB 天塔组：解锁门控 / 进塔耗次 / 塔影前缀 / 首层奖与纪录 / 三层祝福三选一+离塔出口 /
    五层宝箱+三成回血 / 离塔双写纪录 / 败北血线三成无折损 / 成就四档 / 页签双形态渲染；
  - AC 终章组：十章结构与 supR / c10 三段剧情与终战旗标 / 守门人注册 / 卷章 chip「10 章」 /
    beyondGate +3% 生效 / s21 与成就 56 项。
- 既有回归适配：verify-v7 ST4 九章→十章进度轨；verify-v10 F6/E5 支线 20→21 则。
- `node scripts/build.mjs` 通过（50 模块，851 KB）；缓存号 `?v=39`→`?v=40`。
- 九套既有回归 + verify-v11 全绿（0 控制台错误）；`UPDATE_NOTES_V25.md` + README 同步；
  **全部完成后一次性 commit（钩子自动推送双仓库），中途不提交。**

## 已知取舍

- `body:has` 滚动锁在不支持的旧内核上自动降级为现状（页面本就可滚），无副作用；
- 剧情沉浸态全屏为 ≤860px 行为，桌面剧情保持居中卡片；

| 风险 | 对策 |
|---|---|
| story 全屏化影响桌面观感 | 断点 ≥861px 完全不触碰既有规则；桌面仅补纸张背景 |
| body:has 滚动锁旧内核不支持 | 渐进增强：不支持即维持现状，无副作用 |
| 塔战斗污染既有统计 | 只保留 wins + 专用 tower 计数；悬赏/驯兽/图鉴逻辑全部绕开 |
| 祝福改敌人数值破坏平衡 | 祝福只在塔内 ctx.enemy 生效，出塔即弃；层奖随层数线性爬坡 |
| 底部导航文字改动影响文案断言 | 锁定态才图标化，可解锁页签文字原样保留 |
