# 更新日志 · v23「顺手」——QoL 提速 + 防回归

> v22 归一了架构与移动端,v23 把高频操作的点击次数降下来、把修掉的坏块用工具锁死、清掉遗留物。
> 计划全文见 `docs/PLAN_V23.md`。数值与经济依旧零改动。

## 阶段一 操作提速

- **万宝阁批量购买 ×5**:丹药/材料/种子类商品一键连买五件(`ShopSys.buyMulti`),
  逐次按当前市价扣款、灵石不足自动停并入账;功法/法器仍单购。
- **连续探索 ×5**:舆图每张地图卡新增「连续探索 ×5」——至多五次历练,
  **遇战斗/剧情/弹窗/天下大事自动暂停**,处置后可续点(`Explore.goMulti`)。
- **一键行权·自动补种**:一键行权在采收后自动把空田按持有量最多的种子播满
  (种子唯一用途即播种),小账注明播了什么。

## 阶段二 提醒与信息

- **奇市提醒进当前建议**:黑市开市中(余 N 日)、拍卖行拍期将止(余 ≤10 日)→ 一键直达 `shop:odd`。
  连带修瑕:建议栏四条上限在进度中段必然挤掉新知/奇市提醒——**容量提升至五条**。
- **成就页排序**:每分类**未完成(带进度)在前、已完成在后**。
- **战斗回顾留三场**:从"仅上一场"升级为**最近三场**折叠切换(对手/胜负/全程记录;
  会话内存不进存档,`Battle.history`)。

## 阶段三 移动端信息密度

- **顶栏迷你条**:≤860px 顶栏新增**气血/修为双迷你条**(桌面隐藏)——
  道途抽屉收起时也能一眼看血线。

## 阶段四 防回归工具

- **构建期 CSS 体检**:`build.mjs` 新增**花括号平衡 + 注释闭合**检查,任何不平衡直接拒绝构建
  ——把 v22 修掉的"v19 截断坏块吞规则"这类事故用工具永久锁死。
- **verify-v10 新增 Y 组 8 断言(71/71)**:×5 购买足额/不足两态、连续探索自动暂停、
  自动补种、奇市双提醒、成就排序、迷你条渲染与桌面隐藏、三场回顾折叠、CSS 体检接线。

## 阶段五 清理

- 删除遗留截图脚本 `shots-v13.mjs / shots-v14.mjs / shots-v19.mjs`
  (旧版本产物,verify-v10 自带截图助手,git 历史可追回);README 同步。

## 质量收口

- 九套全量回归全绿(392+ 断言,0 控制台错误);`index.html` 缓存号递增 `?v=38`。
- 升级兼容:无新增玩家字段;`Battle.history` 为会话内存,老档无损。

## 开发过程记趣(自测抓获)

- `Explore.goMulti` 初版用 `rfind('};')` 定位插入点——explore.js 里同时住着 Explore 与
  EventSys 两个对象,goMulti 一头扎进了 EventSys 的尾巴,排查半天才揪出来。
